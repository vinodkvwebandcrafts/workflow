import { Widget } from "./interface";

export default {
  hero: async (widget: Widget, pageData?: any) => {
    return widget;
  },
  features: async (widget: Widget, pageData?: any) => {
    return widget;
  },
  testimonial: async (widget: Widget, pageData?: any) => {
    return widget;
  },
  callToAction: async (widget: Widget, pageData?: any) => {
    return widget;
  },
}